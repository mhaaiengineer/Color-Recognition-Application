# Color-Recognition-Application
A Python-based Color Recognition Application using OpenCV and pandas that identifies the closest color name and RGB values from an image through double-click interaction.
